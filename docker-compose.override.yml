version: '3'

services:
  networks:
  web:
    external:
      name: traefik_webgateway
  labels:
    - "traefik.enable=true"
    - "traefik.backend=rename.kr.ua"
    - "traefik.frontend.rule=Host:renamekr.ua, www.renamekr.ua"
    - "traefik.port=80"
